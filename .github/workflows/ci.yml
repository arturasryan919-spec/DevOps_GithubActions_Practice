name: DevOps Practice (Github Actions)
on: push
jobs:
  build:
      - runs-on: Ubuntu:latest
      steps:
        - uses: actions/checkout@v7
        - uses: actions/python@v7.0.0
        run: pip install -r requirements.txt
        run: pytest
